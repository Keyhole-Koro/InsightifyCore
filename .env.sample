# Absolute path to the directory containing repositories to analyze
REPOS_ROOT=/workspaces/Insightify/InsightifyCore/repos

# API Keys for LLM providers (only one is needed depending on --provider flag)
GEMINI_API_KEY=your_gemini_api_key_here
GROQ_API_KEY=your_groq_api_key_here

# Optional: Salt to invalidate cache when changing prompts or models manually
CACHE_SALT=v1