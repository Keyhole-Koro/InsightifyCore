# Copy this file to .env and fill in values.

# Required: API key for Gemini.
GEMINI_API_KEY=

# Optional: Global LLM rate limiting (requests per second and burst).
# If unset or <= 0, throttling is disabled. Burst defaults to 1 when RPS > 0.
# Example: allow 2 requests/sec with a burst of 2
# LLM_RPS=2
# LLM_BURST=2

# Optional: Gemini-specific rate limits. Used only if LLM_* are not set.
# GEMINI_RPS=
# GEMINI_BURST=

GROQ_API_KEY=
